{
  "name": "asterion-one-ground",
  "version": "0.1.0",
  "description": "Asterion One — Ground Segment (API Server + WebSocket Gateway)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest --verbose",
    "migrate": "knex migrate:latest --knexfile src/db/knexfile.js",
    "migrate:rollback": "knex migrate:rollback --knexfile src/db/knexfile.js",
    "lint": "eslint src/"
  },
  "dependencies": {
    "express": "^4.21.0",
    "ws": "^8.16.0",
    "knex": "^3.1.0",
    "pg": "^8.13.0",
    "cors": "^2.8.5",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.56.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
